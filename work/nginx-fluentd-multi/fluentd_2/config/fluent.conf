<source>
  @type tail
  format none
  path /var/log/nginx/access.log
  tag http.log
  pos_file /var/log/nginx/access.log.pos
</source>

<match http.log>
  @type forward
  <server>
    name collectionfluentd
    host fluentd_0
    port 24224
  </server>
</match>
